const darkTheme = {
  background: "#000000",
  textColor: "#fff",
};
const lightTheme = {
  background: "#008888",
  textColor: "#000",
};

const theme = {
  dark: {
    color: darkTheme,
  },
  light: {
    color: lightTheme,
  },
};

export default theme;
